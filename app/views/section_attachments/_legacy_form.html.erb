<%= form_for([manual, section, attachment]) do |f| %>
  <%= f.text_field :title, class: 'form-control' %>
  <%= f.file_field :file %>
  <div class='actions'>
    <button type="submit" class="btn btn-success" data-disable-with="Saving...">Save attachment</button>
  </div>
<% end %>
